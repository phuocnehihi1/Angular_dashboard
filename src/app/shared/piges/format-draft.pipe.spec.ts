import { FormatDraftPipe } from './format-draft.pipe';

describe('FormatDraftPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDraftPipe();
    expect(pipe).toBeTruthy();
  });
});
